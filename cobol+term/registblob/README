= 画像を表示するプログラム

Entryに以下の単語を入力するとその画像を表示する。
不正な単語を入力すると "cat" になる。

 * cat
 * dog
 * cow

= 動作方法

== 起動

$ ./init start

== 終了

$ ./init stop
$ ./init clean

== クライアント設定

アプリケーション指定 -> panda:registblob
ユーザパスワード -> sample:sample
