= visible設定のサンプル

visible switchというボタンをクリックするとウィジェットのvisible属性が反転する

= 対象環境

 * panda-1.4.7

= ビルド

$ make

= 動作方法

== 起動

$ ./init start

== 終了

$ ./init stop
$ ./init clean

== pandaの指定

initlibのPATH,LD_LIBRARY_PATHで指定する。

== クライアント設定

アプリケーション指定 -> panda:testvisible
ユーザパスワード -> sample:sample
