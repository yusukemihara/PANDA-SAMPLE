name    env48;

base    "%PWD%";
ddir    "=/rec";
record  "=/rec";

multiplex_level no;

linkage link;

initial_ld env48;

wfc {
    port      ":9001";
};

sysdata {
    space   "=/blob";
    port    "/tmp/sysdata";
};

ld {
	env48		1;
};
