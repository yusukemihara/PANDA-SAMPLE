= エントリー無効化サンプル

下部Switchボタンをクリックすることで、上部エントリー、ナンバーエントリーの編集有効、無効を切り替える。
またSwitchボタンクリック時にエントリーに入力された文字列をスタイルとして適用する。

= ビルド

$ make

= 動作方法

== 起動

$ ./init start

== 終了

$ ./init stop
$ ./init clean

== pandaの指定

initlibのPATH,LD_LIBRARY_PATHで指定する。

== クライアント設定

アプリケーション指定 -> panda:entrytest
ユーザパスワード -> sample:sample
