= ファイルをダウンロードするプログラム

以下のラベルのボタンをクリックするとダウンロードが開始される。

 * a.odt
 * b.png
 * c.txt

= 動作方法

== 起動

$ ./init start

== 終了

$ ./init stop
$ ./init clean

== クライアント設定

アプリケーション指定 -> panda:download
ユーザパスワード -> sample:sample
